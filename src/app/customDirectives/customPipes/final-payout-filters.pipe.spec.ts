import { FinalPayoutFiltersPipe } from './final-payout-filters.pipe';

describe('FinalPayoutFiltersPipe', () => {
  it('create an instance', () => {
    const pipe = new FinalPayoutFiltersPipe();
    expect(pipe).toBeTruthy();
  });
});
