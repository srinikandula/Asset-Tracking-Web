import { FrentendSortDirective } from './frentend-sort.directive';

describe('FrentendSortDirective', () => {
  it('should create an instance', () => {
    const directive = new FrentendSortDirective();
    expect(directive).toBeTruthy();
  });
});
